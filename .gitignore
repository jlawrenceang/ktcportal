# Secrets — never commit Jotform API keys, Google/Supabase creds
.env
.env.*
!.env.example
*.key
service-account*.json

# Node (Phase 2)
node_modules/
dist/
npm-debug.log*

# build artifact from scripts/apply-theme.sh (logo inlined into CSS)
.theme-combined.css

# Vercel
.vercel

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# OS / editor
.DS_Store
Thumbs.db

.vercel
