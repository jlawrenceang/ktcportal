# Secrets — never commit API keys, Google/Supabase creds
.env
.env.*
*.key
*.pem
*.crt
*.p12
*.p8
*.pfx
service-account*.json

# Node (Phase 2)
node_modules/
dist/
npm-debug.log*
*.tsbuildinfo

# build artifact from scripts/apply-theme.sh (logo inlined into CSS)
.theme-combined.css

# Supabase CLI working state (function-deploy cache, linked-project ref)
supabase/.temp/

# Vercel
.vercel

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# OS / editor
.DS_Store
Thumbs.db

.vercel

# agent worktrees (transient) + local agent config
# (settings.local.json can hold API keys; settings.json is a personal plugin toggle)
.claude/worktrees/
.claude/settings.json
.claude/settings.local.json

# throwaway local debug script (reads .env.local); reusable _apply_one/_ledger_rename stay tracked
scripts/_pushadmin.mjs

# local screenshot capture (not committed)
docs/screenshots/
docs/ktc-screenshots/
docs/ktc-screenshots.zip

# generated translation-review export (regenerate via scripts/export-translations.mjs)
docs/translations-review.csv
docs/translations-review.md

# Android signing keystore + secrets (never commit)
android-keystore.jks
*.jks
*.keystore
android/
*.apk
*.aab

# Generated TWA/Android project (built locally from twa-manifest.json, never committed)
/app/
/gradle/
/gradlew
/gradlew.bat
/build.gradle
/settings.gradle
/gradle.properties
/local.properties
/manifest-checksum.txt
/store_icon.png
.gradle/
/build/
*.idsig

# KTC terminal source photos — huge originals (~13MB each, ~290MB total); only the
# optimized hero.jpg / dash-customer.jpg / dash-admin.jpg are committed + shipped.
public/photos/[0-9]*.jpg
